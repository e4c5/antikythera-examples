variables:
  projects_folder: ${HOME}/csi
  m2_folder: ${HOME}/.m2/repository

# Points to top level folder of the application under test. This is a file system path.
base_path : ${projects_folder}/antikythera-examples/
